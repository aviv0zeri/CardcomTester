# Competition template (do not paste)

Canaan / כנען תיירות Cardcom Low Profile paste, saved for comparison only.

This is **not** a Cardcom production slot and is **not** in `cardcom_export`.
Do not paste these files into Cardcom’s editor.

Local preview:

```
/cardcom-preview/open.html?v=competition-template
```

React tester: **Competition template** (local mock overlay, not `POST /payment`).
