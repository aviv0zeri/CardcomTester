# Archived Low Profile snapshots

Non-production. Do not paste.

## `english-full-snapshot/`

Frozen English redirect HTML + CSS from before dual-mode iframe rules.

## `old-separate-iframe/`

Former `full/iframe/{he,en}` paste files, kept until we confirm whether Cardcom stores a separate iframe vs redirect custom design.

Canonical paste is `../checkout.html` with `../rtl/checkout.css` or `../ltr/checkout.css`.
