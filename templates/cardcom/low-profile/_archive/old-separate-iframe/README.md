Non-production archive of the former `full/iframe/{he,en}` paste files.

Kept until we confirm with Cardcom whether iframe and redirect share one stored custom HTML/CSS slot. Do not paste these. Canonical paste is `../../checkout.html` with `../../rtl/` or `../../ltr/` CSS.
