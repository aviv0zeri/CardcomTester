# Working copy — do not paste

Claude's scratch copy of the compact-iframe redesign. The canonical paste files
stay in the parent folder (`checkout.html`, `rtl/checkout.css`, `ltr/checkout.css`)
and are untouched until the redesign is approved.

Preview this copy with `&wip=1` on any low-profile version:

    /cardcom-preview/open.html?v=low-profile/he/embed&wip=1

`cardcom_export` never sees this folder, so it cannot be pasted by accident.
When approved, the three files are diffed and promoted onto the canonical ones.
